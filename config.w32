// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("wing", "for wing support", "no");

// Otherwise, use ARG_ENABLE
ARG_ENABLE("wing", "enable wing support", "no");

if (PHP_WING != "no") {
	EXTENSION("wing", "wing.c");
}

